# Roton Iced

Iced rewrite experiment for Roton, a Wayland `wl-screenrec` wrapper.

## Run

```sh
cargo run
```

Runtime tools:

- `wl-screenrec` for recording
- `slurp` for area selection
- `ffmpeg` for thumbnails and paused segment concat
- `pactl` / PipeWire Pulse for audio devices
